{
  "collectionName": "components_shared_feature_lists",
  "info": {
    "displayName": "featureList",
    "description": ""
  },
  "options": {},
  "attributes": {
    "featureImage": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": [
        "images",
        "files"
      ]
    },
    "title": {
      "type": "string",
      "regex": "^((?!<[^>]+>).)*$"
    },
    "description": {
      "type": "text",
      "regex": "^((?!<[^>]+>).)*$"
    },
    "subTitle": {
      "type": "string",
      "regex": "^((?!<[^>]+>).)*$"
    },
    "link": {
      "displayName": "link",
      "type": "component",
      "repeatable": false,
      "component": "shared.link"
    }
  }
}
