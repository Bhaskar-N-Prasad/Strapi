{
  "collectionName": "components_shared_benefit_sections",
  "info": {
    "displayName": "benefitSections",
    "description": ""
  },
  "options": {},
  "attributes": {
    "image": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": [
        "images"
      ]
    },
    "title": {
      "type": "string",
      "regex": "^((?!<[^>]+>).)*$"
    },
    "description": {
      "type": "text",
      "regex": "^((?!<[^>]+>).)*$"
    }
  }
}
