{
  "collectionName": "components_shared_benefits",
  "info": {
    "displayName": "benefits",
    "description": ""
  },
  "options": {},
  "attributes": {
    "order": {
      "max": 100,
      "min": 1,
      "type": "integer"
    },
    "sectionDirection": {
      "type": "enumeration",
      "enum": [
        "row",
        "column"
      ]
    },
    "title": {
      "type": "string",
      "regex": "^((?!<[^>]+>).)*$"
    },
    "description": {
      "type": "text",
      "regex": "^((?!<[^>]+>).)*$"
    },
    "sections": {
      "displayName": "benefitSections",
      "type": "component",
      "repeatable": true,
      "component": "shared.benefit-sections"
    }
  }
}
