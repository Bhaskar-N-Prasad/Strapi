{
  "collectionName": "components_shared_links",
  "info": {
    "displayName": "link",
    "description": ""
  },
  "options": {},
  "attributes": {
    "linkText": {
      "type": "string",
      "regex": "^((?!<[^>]+>).)*$"
    },
    "redirectionUrl": {
      "type": "string",
      "regex": "^((?!<[^>]+>).)*$"
    },
    "insightsSection": {
      "type": "string",
      "regex": "^((?!<[^>]+>).)*$"
    }
  }
}
